# Google スプレッドシート連携セットアップ

入力した顧客・請求書データを Google スプレッドシートに蓄積する手順です。

> なぜサーバーが要るのか：Google Sheets API はブラウザから直接安全に呼べません
> （CORS・サービスアカウント鍵の露出）。そのため同梱の軽量サーバー（`server/`）が
> 橋渡しします。鍵はサーバー側だけに置かれ、ブラウザには出ません。

## 手順

### 1. スプレッドシートを用意
1. Google スプレッドシートを新規作成（タブは空でOK。「顧客」「請求書」タブは自動作成されます）
2. URL の `/d/` と `/edit` の間の ID を控える
   `https://docs.google.com/spreadsheets/d/<このIDを控える>/edit`

### 2. サービスアカウントを作成して鍵を取得
1. https://console.cloud.google.com/ でプロジェクトを作成（既存でも可）
2. 「APIとサービス」→「ライブラリ」で **Google Sheets API** を有効化
3. 「認証情報」→「認証情報を作成」→「**サービスアカウント**」を作成
4. 作成したサービスアカウントの「キー」→「鍵を追加」→ **JSON** をダウンロード
5. ダウンロードした JSON をプロジェクト直下に `service-account.json` として置く
   （`.gitignore` 済み。コミットされません）

### 3. スプレッドシートをサービスアカウントに共有
1. `service-account.json` 内の `client_email`（`...@....iam.gserviceaccount.com`）をコピー
2. 手順1のスプレッドシートを開き、右上「共有」でそのメールアドレスを **編集者** として追加

### 4. 環境変数を設定
プロジェクト直下に `.env` を作成（`.env.example` をコピー）：

```
GOOGLE_SPREADSHEET_ID=（手順1のID）
GOOGLE_APPLICATION_CREDENTIALS=./service-account.json
```

### 5. 起動
```bash
npm install
npm run dev:all      # フロント(:5173) と サーバー(:8787) を同時起動
```

初回に「顧客」「請求書」タブとヘッダ行が自動作成されます。
サイドバー下部が「**スプレッドシート同期中**」（緑）になれば成功です。
未設定・サーバー未起動のときは「ローカル保存」となり、従来どおり localStorage で動きます。

## シートの構成

### 顧客タブ
| id | 企業名 | 担当者 | メール | 住所 | 電話 |
| --- | --- | --- | --- | --- | --- |

### 請求書タブ
| id | 請求書番号 | 発行日 | 支払期限 | ステータス | 請求者 | 課税区分 | 顧客ID | 顧客名 | 小計 | 消費税 | 合計 | 登録番号 | 明細JSON | 請求者JSON | 備考 | 作成日時 | 更新日時 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |

- `id` 列をキーに **upsert**（同じ請求書の再保存は同じ行を更新）します。
- 明細と請求者は、往復復元のため JSON 列にも保持します（人が見る用の列は別途あり）。

## 同期の挙動
- **書き込み**：作成・編集・削除のたびに localStorage へ即時保存しつつスプレッドシートへも反映
  （write-through, best-effort）。シート側が失敗しても UI は止まりません。
- **読み込み**：起動時に連携が有効なら顧客・請求書をシートから取得して表示します。
- **請求者プロファイル（5件）** はアプリ設定として端末側に保持します。

## デプロイ
`server/index.mjs` は素の Node/Express です。Render・Railway・Fly.io 等へデプロイし、
鍵は `GOOGLE_SERVICE_ACCOUNT_JSON` 環境変数（JSON 丸ごと）で渡すのが簡単です。
