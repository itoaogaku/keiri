# Notion 連携セットアップ

入力した顧客・請求書データを Notion のデータベースに保存する手順です。

> なぜサーバーが要るのか：Notion API はブラウザから直接呼べません（CORS 非対応＋
> シークレットトークンを画面に出せない）。そのため同梱の軽量サーバー（`server/`）が
> 橋渡しします。トークンはサーバー側の環境変数だけに置かれ、ブラウザには出ません。

## 手順

### 1. Notion インテグレーションを作成
1. https://www.notion.so/my-integrations を開く
2. 「New integration」→ 種類は **Internal**、ワークスペースを選択
3. 作成後の **Internal Integration Secret**（`secret_...` または `ntn_...`）をコピー

### 2. 保存先の親ページを用意して接続
1. Notion で任意のページを1つ作る（例：「Keiri データ」）
2. そのページ右上「**…**」→「**コネクトを追加**」→ 手順1のインテグレーションを選択
3. ページ URL 末尾の 32 桁 ID を控える
   `https://www.notion.so/Keiri-データ-<この32桁がページID>`

### 3. 環境変数を設定
プロジェクト直下に `.env` を作成（`.env.example` をコピー）：

```
NOTION_TOKEN=（手順1のシークレット）
NOTION_PARENT_PAGE_ID=（手順2の32桁ID）
```

### 4. 起動
```bash
npm install
npm run dev:all      # フロント(:5173) と サーバー(:8787) を同時起動
```

初回起動時、親ページの下に **「Keiri 顧客」「Keiri 請求書」** の2つのデータベースが
自動作成されます（スキーマも自動設定）。作成された DB ID は `server/.notion-dbs.json`
にキャッシュされ、次回以降は再利用されます。

サイドバー下部が「**Notion 同期中**」（緑）になれば連携成功です。
未設定・サーバー未起動のときは「ローカル保存」となり、従来どおり localStorage で動きます。

## データベースのスキーマ

### Keiri 顧客
| プロパティ | 型 |
| --- | --- |
| 企業名 | タイトル |
| 担当者 / メール / 住所 / 電話 | テキスト |
| AppID | テキスト（アプリ内部ID・同期キー） |

### Keiri 請求書
| プロパティ | 型 |
| --- | --- |
| 請求書番号 | タイトル |
| 発行日 / 支払期限 | 日付 |
| ステータス | セレクト（下書き/発行済み/入金待ち/入金済み/延滞） |
| 請求者 | セレクト（5つの請求者名） |
| 課税区分 | セレクト（課税 / 非課税） |
| 顧客 / 顧客ID / 登録番号 / 備考 | テキスト |
| 小計 / 消費税 / 合計 | 数値（¥） |
| 明細JSON / 請求者JSON | テキスト（往復復元用の JSON） |
| AppID | テキスト（同期キー） |

## 同期の挙動
- **書き込み**：作成・編集・削除のたびに、localStorage へ即時保存しつつ Notion へも反映
  （write-through, best-effort）。Notion 側が失敗しても UI は止まりません。
- **読み込み**：起動時に Notion が有効なら顧客・請求書を Notion から取得して表示します。
- **請求者プロファイル（5件）** はアプリ設定として端末側に保持します。

## デプロイ
`server/index.mjs` は素の Node/Express なので、Render・Railway・Fly.io 等へそのまま
デプロイできます。フロントは `npm run build` の `dist/` を静的配信し、`/api` を
サーバーへ向ければ本番でも同じ構成で動きます。
