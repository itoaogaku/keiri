# スプレッドシート連携（Google Apps Script 方式）

サーバーも鍵（JSON）も不要で、入力データをスプレッドシートに蓄積します。
必要なのは「スプレッドシートにスクリプトを1回貼る」ことだけです。

## 全体像

```
画面（Vercelで公開） ──直接通信──▶ スプレッドシートに貼ったスクリプト（GAS） ──▶ スプレッドシート
```

- Node.js のインストール不要（Vercel がクラウドでビルド）
- サービスアカウント・JSON鍵 不要（組織ポリシーの影響を受けません）

---

## 手順1：スプレッドシートにスクリプトを貼る

1. データを蓄積したいスプレッドシートを開く
2. 上部メニュー「**拡張機能**」→「**Apps Script**」
3. エディタの内容をすべて消し、リポジトリの **`apps-script/Code.gs`** の中身を丸ごと貼り付け
4. 保存（💾 または Ctrl/⌘+S）

## 手順2：ウェブアプリとして公開

1. 右上「**デプロイ**」→「**新しいデプロイ**」
2. 歯車⚙「種類の選択」→「**ウェブアプリ**」
3. 設定：
   - 説明：任意（例：keiri）
   - **次のユーザーとして実行：自分**
   - **アクセスできるユーザー：全員**
4. 「**デプロイ**」→ 初回は「アクセスを承認」を求められるので、自分のアカウントで許可
   （「このアプリは確認されていません」が出たら「詳細」→「（プロジェクト名）に移動」で続行）
5. 表示される「**ウェブアプリ URL**」（`https://script.google.com/macros/s/.../exec`）をコピー

> スクリプトを後で編集したら、「デプロイ」→「デプロイを管理」→ 鉛筆✏ →
> バージョンを「新バージョン」にして再デプロイしてください（URL は変わりません）。

## 手順3：アプリに URL を貼る

1. Keiri を開き、左メニュー「**設定**」
2. 「**スプレッドシート連携**」欄に手順2の URL を貼って「**接続**」
3. バッジが「**接続中**」（緑）になれば完了。以降、作成・編集・削除がシートに蓄積されます。

「顧客」「請求書」シートとヘッダ行は自動で作られます。

---

## Vercel で公開する（任意）

自分の PC で動かさず、ブラウザだけで使いたい場合：

1. この GitHub リポジトリを Vercel で **Import**
2. フレームワークは自動で **Vite** と認識されます（Build: `npm run build` / Output: `dist`）
3. 「Deploy」を押すだけ。発行された URL がそのままアプリの公開先です
4. アプリを開いて上記「手順3」で GAS URL を接続

> URL を毎回貼るのが面倒なら、Vercel の Project → Settings → Environment Variables に
> `VITE_GAS_URL = （ウェブアプリURL）` を登録して再デプロイすると、最初から接続済みになります。

## 同期の挙動

- **書き込み**：作成・編集・削除のたびに、この端末（localStorage）へ即時保存しつつ、
  スプレッドシートへも反映します（失敗しても画面は止まりません）。
- **読み込み**：接続中なら起動時に顧客・請求書をシートから取得して表示します。
- **請求者プロファイル（5件）** はアプリ設定として端末側に保持します。
